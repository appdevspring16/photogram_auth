<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>PhotogramAuth</title>

  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootswatch/3.3.5/flatly/bootstrap.min.css">
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">

  <style>
    /* Only necessary if you are using navbar-fixed-top */
    body {
      padding-top: 80px;
    }
  </style>

  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

  <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>

  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
</head>
<body>
  <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a href="/" class="navbar-brand">PhotogramAuth</a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a href="/users"><i class="fa fa-users" aria-hidden="true"></i> Users</a></li>
          <li>
            <% if current_user.present? %>
            <a href="/users/<%= current_user.id %>"><i class="fa fa-user" aria-hidden="true"></i></i> My wall</a>
            <% else %>
            <% end %>
          </li>
          <li>
            <% if current_user.present? %>
            <a href="/my_likes"><i class="fa fa-heart" aria-hidden="true"></i></i> My likes</a>
            <% else %>
            <% end %>
          </li>
          <li><a href="/comments">Comments</a></li>
          <li><a href="/likes">Likes</a></li>
          <li><a href="/photos">Photos</a></li>
          <li><a href="/rails/info">Sitemap</a></li>
        </ul>

        <ul class="nav navbar-nav navbar-right">

          <li>
            <% if current_user.present? %>
            <a href="/users/edit"><i class="fa fa-pencil" aria-hidden="true"></i> <%= current_user.username %></a>
            <% else %>
            <a href="/users/sign_in"><i class="fa fa-pencil" aria-hidden="true"></i>Sign In</a>
            <% end %>
          </li>

          <li>
            <% if current_user.present? %>
            <a href="/users/sign_out" data-method="delete" rel="nofollow"><i class="fa fa-sign-out" aria-hidden="true"></i> Sign Out</a>
            <% else %>
            <a href="/users/sign_up" data-method="delete" rel="nofollow"><i class="fa fa-sign-out" aria-hidden="true"></i> Sign Up</a>
            <% end %>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div>
  </nav>

  <div class="container">
    <% if notice.present? %>
      <div class="alert alert-dismissable alert-success">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= notice %>
      </div>
    <% end %>


    <% if alert.present? %>
      <div class="alert alert-dismissable alert-warning">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= alert %>
      </div>
    <% end %>


    <%= yield %>
  </div>
</body>
</html>
