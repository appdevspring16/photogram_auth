<div class="page-header">
  <h3>My Likes</h3>
</div>

<div class="row">

  <% @myLike.each do |like| %>

  <% liked_photo = like.photo_id %>

  <div class="col-md-12">
    <dl class="dl-horizontal">
      <dt><h4><%= User.find_by( :id => Photo.find_by( :id => liked_photo).user_id ).username %></h4></dt>
      <dd><h5><%= Photo.find_by( :id => liked_photo).caption %></h5></dd>

      <dt></dt>
      <dd><img src="<%= Photo.find_by( :id => liked_photo).image %>" height="300" alt="Photo not found"></img></dd>
    </dl>

  </div>

  <% end %>

</div>
