<div class="page-header">
  <h1>
    Users
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <% @users.each do |user| %>
      <tr>
        <td><%= user.username %></td>
          <a href="/users/<%= user.id %>" class="btn btn-primary">Show</a>

        </td>
      </tr>
      <% end %>
  </div>
</div>
