<% @likes.each do |like| %>
<div class="col-md-8 col-md-offset-2">
<div class="panel panel-default">
  <div class="panel-heading"> <h3 class="panel-title"><%= like.user.username %></h3>
    </div>
  <div class="panel-body">
    <div>
      <img src="<%= like.photo.image %>" class="img-responsive" alt="Responsive image">
    </div>
    <!-- <p>
      <%= like.user.username %> likes this
    </p> -->
    <!-- <div class="media-body">
      <h6 class="media-heading"><%= like.user.username %></h6>
      <p>
          <%= like.photo.caption %>
      </p>
    </div> -->
    </div>
</div>
</div>
<% end %>


<!--
<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>User</th>
        <th>Photo</th>
        <th>Actions</th>
      </tr>

      <% @likes.each do |like| %>
      <tr>
        <td><%= like.user_id %></td>
        <td><%= like.photo_id %></td>
        <td>
          <a href="/likes/<%= like.id %>" class="btn btn-primary">Show</a>
          <a href="/likes/<%= like.id %>/edit" class="btn btn-warning">Edit</a>
          <a href="/delete_like/<%= like.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div> -->
